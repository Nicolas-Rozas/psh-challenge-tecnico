/** @type {import('next').NextConfig} */
const nextConfig = {
    compiler:{
        styledComponents:true
    },
    images: {
        domains: ['randomuser.me'],
      },
};

export default nextConfig;
